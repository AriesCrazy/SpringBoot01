<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.by.dao.DepartmentDao">
    <resultMap type="com.by.entity.Department" id="DepartmentMap">
        <result property="id" column="id" />
        <result property="name" column="name" />
        <result property="manager" column="manager" />
        <result property="phone" column="phone" />
        <result property="status" column="status" />
        <result property="createTime" column="createTime" />
        <result property="lastUpdateBy" column="lastUpdateBy" />
        <result property="lastUpdateTime" column="lastUpdateTime" />
        <result property="brief" column="brief" />
    </resultMap>
    
    
    <!--通用查询-->
    <select id="select" resultMap="DepartmentMap">
        select
            id,name,manager,phone,status,createTime,lastUpdateBy,lastUpdateTime,brief
        from department
        <where>
            <if test="id != null and id != ''">
                and id = #{id}
            </if>
            <if test="name != null and name != ''">
                and name like concat('%',#{name},'%')
            </if>
            <if test="status != null and status != ''">
                and status = #{status}
            </if>
        </where>
    </select>
    
    <!--新增数据-->
    <insert id="insert" keyProperty="id" useGeneratedKeys="true">
        insert into department(id,name,manager,phone,status,lastUpdateBy,brief)
        values (#{id},#{name},#{manager},#{phone},#{status},#{lastUpdateBy},#{brief})
    </insert>
    
    <!-- 批量新增数据 -->
    <insert id="insertBatch" keyProperty="id" useGeneratedKeys="true">
        insert into department(id,name,manager,phone,status,lastUpdateBy,brief)
        values
        <foreach collection="entities" item="entity" separator=",">
            (#{entity.id},#{entity.name},#{entity.manager},#{entity.phone},#{entity.status},#{entity.lastUpdateBy},#{entity.brief})
        </foreach>
    </insert>
    
    <!-- 更新数据 -->
    <update id="update">
        update department
        <set>
            <if test="id != null and id != ''">
                id = #{id},
            </if>
            <if test="name != null and name != ''">
                name = #{name},
            </if>
            <if test="manager != null and manager != ''">
                manager = #{manager},
            </if>
            <if test="phone != null and phone != ''">
                phone = #{phone},
            </if>
            <if test="status != null and status != ''">
                status = #{status},
            </if>
            <if test="createTime != null and createTime != ''">
                createTime = #{createTime},
            </if>
            <if test="lastUpdateBy != null and lastUpdateBy != ''">
                lastUpdateBy = #{lastUpdateBy},
            </if>
            <if test="lastUpdateTime != null and lastUpdateTime != ''">
                lastUpdateTime = #{lastUpdateTime},
            </if>
            <if test="brief != null and brief != ''">
                brief = #{brief},
            </if>
        </set>
        where id = #{id}
    </update>
    
    <!--通过主键软删除-->
    <update id="delete">
        update  department set status = 2 where id = #{id}
    </update>
</mapper>